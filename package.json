{
  "name": "classlog",
  "version": "1.4.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "web:prepare": "node scripts/prepare-web-assets.js",
    "mobile:add:android": "npm run web:prepare && npx cap add android",
    "mobile:sync": "npm run web:prepare && node node_modules/@capacitor/cli/bin/capacitor sync android",
    "mobile:assets": "npx capacitor-assets generate --android",
    "mobile:android": "npm run mobile:sync && npx cap open android",
    "mobile:build:android": "npm run mobile:sync && cd android && ./gradlew assembleRelease",
    "mobile:check": "node scripts/check-widget-layout.mjs",
    "android:release": "bash scripts/release-android.sh",
    "test": "node scripts/check-app-logic.js && node scripts/check-widget-layout.mjs"
  },
  "dependencies": {
    "@capacitor/android": "^7.4.2",
    "@capacitor/core": "^7.4.2"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^7.4.2"
  }
}
